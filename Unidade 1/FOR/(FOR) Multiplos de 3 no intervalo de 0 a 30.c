#include <stdio.h>

int main()
{
    int div;
    int i;

    for (i = 0; i < 30; i++)
    {
        if (i % 3 == 0)
        printf("O numero %d eh um multiplo de 3: \n", i);
    }
    
return 0;
}