#include <stdio.h>

int main(){
    int n1, n2;

    printf("Digite os dois numeros: ");
    scanf("%d" "%d", &n1, &n2);

    if (n1 > n2)
    printf("O %d eh maior que o %d", n1, n2);
    else 
    printf("O %d eh maior que o %d", n2, n1);

return 0;
}