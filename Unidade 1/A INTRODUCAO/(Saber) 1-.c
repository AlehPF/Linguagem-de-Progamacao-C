/*Igual a: == (O operador igual a ( == ) retorna true se ambos os operandos tiverem o mesmo valor; caso contrário, ele retornará false.)
Diferente: !=
&& (E lógico, AND)
|| (OU lógico, OR) 
! (NAO lógico, também chamado de negação lógica, NOT)
for(variavel = inicio; fim; incrimento)
for(cont = 0; cont <= 5; cont++)
c+= 3 EH IGUAL A c=c+3 ou seja VARIAVEL(c) OPERADOR(+) = EXPRESSAO(3)
c++ EH IGUAL A c=c+c */
